# API Configuration
# Use empty string to leverage Vite proxy in development
# This avoids CORS issues by proxying requests through the Vite dev server
VITE_API_BASE_URL=
VITE_WS_BASE_URL=ws://localhost:8080
